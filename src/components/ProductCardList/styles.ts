import styled from 'styled-components'

export const List = styled.ul`
  max-width: 1024px;
  width: 100%;
  margin: 56px auto 0;

  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 48px 32px;

  list-style: none;
`
